# Incidents Dashboard (React + TypeScript + SCSS + PLAYWRITE)

## Quick start

```bash
pnpm install
pnpm run dev
```

Then open the printed local URL.

## Commands

```bash
pnpm run lint
pnpm run format
pnpm run test:unit
pnpm run test:e2e
pnpm run build
pnpm run preview
```

